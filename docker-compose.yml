version: "3.9"

services:
  web:
    build: .
    command: bash -c "bundle install && rails server -b 0.0.0.0 -p 3000"
    volumes:
      - .:/app
    ports:
      - "3000:3000"
    environment:
      RAILS_ENV: development
      # Rails usará SQLite local
    depends_on:
      - postgres

  postgres:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: solmmerce_user
      POSTGRES_PASSWORD: S1o2l3@@##!
      POSTGRES_DB: solmmerce_bd
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: